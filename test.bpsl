import <Vec3>;
import <Vec4>;
import <Trigonometry>;

const float f = 0.5f * 1 + 2 + 3 * 1 / 1 * (3 + 1);
const double d = 40 + 10 - (2*40) + (100/40) + 0.2;

struct MyFuck
{
	int fuck;
	float bullshit;
}

struct constbuf TheFuckingConstBuf
{
	float v3[3];
	float v4[4];
}

struct vlayout Vertex
{
	float Pos[3];
	float NormalPos[3];
	float TexturePos[2];
}

class Vec2
{
	float X;
	float Y;

	Vec2(float x, float y)
	{
		this.X = x;
		this.Y = y;
	}

	Vec2 operator-()
	{
		Vec2 res;

		res.X = -this.X;
		res.Y = -this.Y;
		return (res);
	}
}

const float Zero = 0.0f;

int main()
{
	int i = 0;
	float v = 0.0f;

	Vertex.Pos[0] = 0;
	Vertex.Pos[1] = 0;
	Vertex.Pos[2] = 0;
	Vec3 v3 = Vec3(Zero, v, Vertex.Pos[2]);
	i += 0 * 1 + 2 * 4 - 8 / 1 * 1 + d;
	Vec2 v = Vec2(0.0f, 0.0f);
	Vec4 vv = Vec4(v3.X, v3.Y, v3.Z, 1.0f);
	return (0);
}
