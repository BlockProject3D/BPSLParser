const float f = 0.5f * 1 + 2 + 3 * 1 / 1 * (3 + 1);
const double d = 40 + 10 - (2*40) + (100/40) + 0.2;

struct MyFuck
{
	int fuck;
	float bullshit;
}

struct constbuf TheFuckingConstBuf
{
	float v3[3];
	float v4[4];
}

struct vlayout Vertex
{
	float Pos[3];
	float NormalPos[3];
	float TexturePos[2];
}

class Vec2
{
	float X;
	float Y;

	Vec2 __negate()
	{
		Vec2 res;

		res.X = -this.X;
		res.Y = -this.Y;
		return (res);
	}
}

int main()
{
	int i = 0;

	Vertex.Pos[0] = 0;
	Vertex.Pos[1] = 0;
	Vertex.Pos[2] = 0;
	i += 0 * 1 + 2 * 4 - 8 / 1 * 1 + d;
	return (0);
}
